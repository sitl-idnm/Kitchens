# Copy to .env.local and fill real values (secrets stay out of git).

# --- Public (exposed to the browser) ---
NEXT_PUBLIC_YM_ID=110557445                 # Yandex.Metrika counter id

# --- Secret (server-only; set in .env.local locally or host env in prod) ---
TELEGRAM_BOT_TOKEN=your-telegram-bot-token  # BotFather token
TELEGRAM_CHAT_ID=-100XXXXXXXXXX             # target chat/channel id for leads
